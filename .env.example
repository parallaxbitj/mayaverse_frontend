# EmailJS Configuration
# 1. Create account at https://dashboard.emailjs.com/
# 2. Add an Email Service (e.g., Gmail) and copy the "Service ID"
# 3. Create an Email Template with '{{otp}}' and '{{to_email}}' variables, then copy the "Template ID"
# 4. Go to Account -> API Keys and copy the "Public Key"
CCC